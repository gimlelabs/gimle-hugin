name: move
description: "Move in a direction (north, south, east, west, northeast, northwest, southeast, southwest). You can also use abbreviations: n, s, e, w, ne, nw, se, sw."
parameters:
  world_id:
    type: string
    description: The ID of the world you are in
    required: true
  direction:
    type: string
    description: "Direction to move (north, south, east, west, northeast, northwest, southeast, southwest, or n, s, e, w, ne, nw, se, sw)"
    required: true
  reason:
    type: string
    description: Why you are performing this action
    required: false
is_interactive: false
implementation_path: the_hugins.tools.move:move_tool
options: {}
