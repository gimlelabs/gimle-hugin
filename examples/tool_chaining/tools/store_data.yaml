name: store_data
description: |
  Store transformed data. This is the final step in the pipeline.
  This tool is typically called automatically by transform_data.
implementation_path: tool_chaining.tools.pipeline_tools:store_data
parameters:
  transformed_data:
    type: string
    description: JSON string of transformed data to store
    required: true
