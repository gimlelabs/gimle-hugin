name: validate_data
description: |
  Validate input data. If valid, automatically chains to transform_data
  without returning to the LLM.
implementation_path: tool_chaining.tools.pipeline_tools:validate_data
parameters:
  data:
    type: string
    description: JSON string of data to validate
    required: true
